<UserControl x:Class="StudentManagement.WPF.Views.DashboardView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008">
    <UserControl.Resources>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="20,16"/>
            <Setter Property="Margin" Value="0,0,15,15"/>
            <Setter Property="BorderBrush" Value="#C9A227"/>
            <Setter Property="BorderThickness" Value="0,3,0,0"/>
        </Style>
        <Style x:Key="CardLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#6B7280"/>
        </Style>
        <Style x:Key="CardValue" TargetType="TextBlock">
            <Setter Property="FontSize" Value="32"/>
            <Setter Property="FontFamily" Value="Georgia"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#1E3A5F"/>
        </Style>
    </UserControl.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
            <Viewbox Width="28" Height="28" Margin="0,0,10,0">
                <Path Data="{StaticResource GraduationCapGeometry}" Fill="#1E3A5F" Stretch="Uniform"/>
            </Viewbox>
            <TextBlock Text="System Dashboard" Style="{StaticResource PageTitleStyle}"/>
        </StackPanel>

        <WrapPanel Grid.Row="1">
            <Border Style="{StaticResource CardStyle}" Width="200">
                <StackPanel>
                    <TextBlock Text="Total Students" Style="{StaticResource CardLabel}"/>
                    <TextBlock Text="{Binding TotalStudents}" Style="{StaticResource CardValue}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CardStyle}" Width="200">
                <StackPanel>
                    <TextBlock Text="Active Students" Style="{StaticResource CardLabel}"/>
                    <TextBlock Text="{Binding ActiveStudents}" Style="{StaticResource CardValue}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CardStyle}" Width="200">
                <StackPanel>
                    <TextBlock Text="Graduated Students" Style="{StaticResource CardLabel}"/>
                    <TextBlock Text="{Binding GraduatedStudents}" Style="{StaticResource CardValue}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CardStyle}" Width="200">
                <StackPanel>
                    <TextBlock Text="Lecturers" Style="{StaticResource CardLabel}"/>
                    <TextBlock Text="{Binding TotalLecturers}" Style="{StaticResource CardValue}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CardStyle}" Width="200">
                <StackPanel>
                    <TextBlock Text="Classes" Style="{StaticResource CardLabel}"/>
                    <TextBlock Text="{Binding TotalClasses}" Style="{StaticResource CardValue}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CardStyle}" Width="200">
                <StackPanel>
                    <TextBlock Text="Majors" Style="{StaticResource CardLabel}"/>
                    <TextBlock Text="{Binding TotalMajors}" Style="{StaticResource CardValue}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CardStyle}" Width="200">
                <StackPanel>
                    <TextBlock Text="Subjects" Style="{StaticResource CardLabel}"/>
                    <TextBlock Text="{Binding TotalSubjects}" Style="{StaticResource CardValue}"/>
                </StackPanel>
            </Border>
        </WrapPanel>
    </Grid>
</UserControl>
