<Window x:Class="StudentManagement.WPF.Views.CourseManagementView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Course Management" Height="700" Width="1050" Background="{StaticResource ParchmentBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Course Management" Style="{StaticResource PageTitleStyle}" Margin="0,0,0,15"/>

        <TabControl Grid.Row="1" Background="White" BorderBrush="#E5E7EB" BorderThickness="1" Padding="10">
            <!-- Subjects Tab -->
            <TabItem Header="Subjects" FontSize="16">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBox Width="150" Height="30" Padding="5,0" VerticalContentAlignment="Center" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0" ToolTip="Search"/>
                        <Button Content="Refresh" Command="{Binding LoadDataCommand}" Width="80" Height="30" Margin="0,0,10,0"/>
                        <Button Content="Deactivate" Command="{Binding DeactivateSubjectCommand}" Width="90" Height="30" Background="#EF4444" Foreground="White"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBox Width="100" Height="30" Padding="5,0" VerticalContentAlignment="Center" Text="{Binding NewSubjectId}" Margin="0,0,10,0" ToolTip="Subject ID"/>
                        <TextBox Width="180" Height="30" Padding="5,0" VerticalContentAlignment="Center" Text="{Binding NewSubjectName}" Margin="0,0,10,0" ToolTip="Subject Name"/>
                        <TextBox Width="60" Height="30" Padding="5,0" VerticalContentAlignment="Center" Text="{Binding NewSubjectCredits}" Margin="0,0,10,0" ToolTip="Credits"/>
                        <Button Content="Add Subject" Command="{Binding AddSubjectCommand}" Width="100" Height="30" Background="#10B981" Foreground="White"/>
                    </StackPanel>

                    <DataGrid Grid.Row="2" ItemsSource="{Binding Subjects}" SelectedItem="{Binding SelectedSubject}"
                              AutoGenerateColumns="False" IsReadOnly="True" RowHeight="35" AlternatingRowBackground="#F9FAFB">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Subject ID" Binding="{Binding SubjectId}" Width="120"/>
                            <DataGridTextColumn Header="Name" Binding="{Binding SubjectName}" Width="250"/>
                            <DataGridTextColumn Header="Credits" Binding="{Binding Credits}" Width="80"/>
                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <!-- Course Sections Tab -->
            <TabItem Header="Course Sections" FontSize="16">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <Button Content="Refresh" Command="{Binding LoadDataCommand}" Width="80" Height="30" Margin="0,0,10,0"/>
                        <ComboBox ItemsSource="{Binding Lecturers}" DisplayMemberPath="FullName" SelectedItem="{Binding LecturerToAssign}" Width="150" Height="30" Margin="0,0,10,0" ToolTip="Lecturer to assign"/>
                        <Button Content="Assign Lecturer" Command="{Binding AssignLecturerCommand}" Width="110" Height="30" Background="#8B5CF6" Foreground="White"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBox Width="90" Height="30" Padding="5,0" VerticalContentAlignment="Center" Text="{Binding NewSectionId}" Margin="0,0,10,0" ToolTip="Section ID"/>
                        <ComboBox ItemsSource="{Binding Subjects}" DisplayMemberPath="SubjectName" SelectedItem="{Binding NewSectionSubject}" Width="140" Height="30" Margin="0,0,10,0" ToolTip="Subject"/>
                        <ComboBox ItemsSource="{Binding Semesters}" DisplayMemberPath="SemesterName" SelectedItem="{Binding NewSectionSemester}" Width="120" Height="30" Margin="0,0,10,0" ToolTip="Semester"/>
                        <TextBox Width="60" Height="30" Padding="5,0" VerticalContentAlignment="Center" Text="{Binding NewSectionRoom}" Margin="0,0,10,0" ToolTip="Room"/>
                        <TextBox Width="80" Height="30" Padding="5,0" VerticalContentAlignment="Center" Text="{Binding NewSectionDay}" Margin="0,0,10,0" ToolTip="Day of week"/>
                        <TextBox Width="60" Height="30" Padding="5,0" VerticalContentAlignment="Center" Text="{Binding NewSectionStartTime}" Margin="0,0,5,0" ToolTip="Start time (HH:mm)"/>
                        <TextBlock Text="-" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <TextBox Width="60" Height="30" Padding="5,0" VerticalContentAlignment="Center" Text="{Binding NewSectionEndTime}" Margin="0,0,10,0" ToolTip="End time (HH:mm)"/>
                        <TextBox Width="60" Height="30" Padding="5,0" VerticalContentAlignment="Center" Text="{Binding NewSectionCapacity}" Margin="0,0,10,0" ToolTip="Capacity"/>
                        <Button Content="Add Section" Command="{Binding AddSectionCommand}" Width="100" Height="30" Background="#10B981" Foreground="White"/>
                    </StackPanel>

                    <DataGrid Grid.Row="2" ItemsSource="{Binding Sections}" SelectedItem="{Binding SelectedSection}"
                              AutoGenerateColumns="False" IsReadOnly="True" RowHeight="35" AlternatingRowBackground="#F9FAFB">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Section ID" Binding="{Binding SectionId}" Width="100"/>
                            <DataGridTextColumn Header="Subject" Binding="{Binding Subject.SubjectName}" SortMemberPath="Subject.SubjectName" Width="200"/>
                            <DataGridTextColumn Header="Lecturer" Binding="{Binding Lecturer.FullName}" SortMemberPath="Lecturer.FullName" Width="150"/>
                            <DataGridTextColumn Header="Room" Binding="{Binding Room}" Width="80"/>
                            <DataGridTextColumn Header="Day" Binding="{Binding DayOfWeek}" Width="80"/>
                            <DataGridTextColumn Header="Time" Binding="{Binding StartTime}" Width="150"/>
                            <DataGridTextColumn Header="Capacity" Binding="{Binding Capacity}" Width="80"/>
                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
