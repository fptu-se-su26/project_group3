<Application x:Class="StudentManagement.WPF.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>
        <ResourceDictionary>

            <!-- School theme palette -->
            <SolidColorBrush x:Key="NavyBrush" Color="#1E3A5F"/>
            <SolidColorBrush x:Key="NavyDarkBrush" Color="#152A47"/>
            <SolidColorBrush x:Key="GoldBrush" Color="#C9A227"/>
            <SolidColorBrush x:Key="MaroonBrush" Color="#7C2D12"/>
            <SolidColorBrush x:Key="ParchmentBrush" Color="#F6F3EC"/>
            <SolidColorBrush x:Key="CardBrush" Color="#FFFFFF"/>
            <SolidColorBrush x:Key="TextDarkBrush" Color="#1F2937"/>
            <FontFamily x:Key="HeadingFont">Georgia</FontFamily>

            <!-- Graduation-cap glyph (Material Design "school" icon, 24x24 viewBox) -->
            <Geometry x:Key="GraduationCapGeometry">M12 3L1 9l4 2.18v6L12 21l7-3.82v-6l2-1.09V17h2V9L12 3zm6.82 6L12 12.72 5.18 9 12 5.28 18.82 9zM17 15.99l-5 2.73-5-2.73v-3.72L12 15l5-2.73v3.72z</Geometry>

            <!-- Page title style: serif, navy, with a gold underline via Border in each view -->
            <Style x:Key="PageTitleStyle" TargetType="TextBlock">
                <Setter Property="FontFamily" Value="{StaticResource HeadingFont}"/>
                <Setter Property="FontSize" Value="26"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Foreground" Value="{StaticResource NavyBrush}"/>
            </Style>

            <!-- DataGrid header: navy background, white serif-ish bold text (applies app-wide) -->
            <Style TargetType="DataGridColumnHeader">
                <Setter Property="Background" Value="{StaticResource NavyBrush}"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Padding" Value="10,8"/>
                <Setter Property="BorderBrush" Value="{StaticResource NavyDarkBrush}"/>
                <Setter Property="BorderThickness" Value="0,0,1,0"/>
            </Style>

            <!-- TabItem: gold underline on selection -->
            <Style TargetType="TabItem">
                <Setter Property="Padding" Value="14,8"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TabItem">
                            <Border x:Name="Border" BorderThickness="0,0,0,3" BorderBrush="Transparent" Padding="{TemplateBinding Padding}" Margin="0,0,4,0">
                                <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource GoldBrush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>
